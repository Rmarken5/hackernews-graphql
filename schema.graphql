type Query{
    link(id: ID!): Link
}

type Mutation{
    deleteLink(id: ID!): Link
    updateLink(id: ID!, url: String, description: String): Link
}

type Link{
    id: ID!
    url: String!
    description: String!
}